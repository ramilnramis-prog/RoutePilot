"""Web-workspace tests (Stage 4 U15).

The package marker exists so ``python -m unittest discover -s tests -t .`` finds
``tests.web.test_web_workspace`` under the same top-level layout as ``tests/api``,
``tests/engine``, ``tests/model``, ``tests/storage``, ``tests/time`` and ``tests/tools``.
"""
